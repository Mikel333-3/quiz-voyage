import { createFileRoute } from "@tanstack/react-router";
import { Pencil, Trophy, Zap } from "lucide-react";
import { AppShell } from "@/components/quiz/AppShell";
import { Avatar, Panel, SectionTitle, StatTile, XpBar } from "@/components/quiz/ui";
import { useGame, levelProgress } from "@/lib/game-store";

export const Route = createFileRoute("/profil")({ component: ProfilePage });
function ProfilePage() { const { player, level, successRate } = useGame(); const p = levelProgress(player.xp); return <AppShell><div className="mx-auto max-w-3xl space-y-5"><Panel glow className="p-6"><div className="flex flex-col items-center gap-4 text-center sm:flex-row sm:text-left"><Avatar name={player.name} size={76} /><div className="min-w-0 flex-1"><p className="font-display text-xs uppercase tracking-[0.3em] text-accent">Player profile</p><h1 className="mt-1 text-2xl font-black">{player.name}</h1><p className="text-sm text-muted-foreground">{player.school}</p></div><button className="tap glass rounded-xl p-3"><Pencil className="size-4" /></button></div><div className="mt-6 flex items-center gap-3"><span className="grid size-12 place-items-center rounded-2xl bg-[image:var(--gradient-primary)] font-display font-black text-primary-foreground">{level}</span><div className="flex-1"><div className="flex justify-between text-xs"><span>Niveau {level}</span><span>{p.inLevel}/{p.needed} XP</span></div><XpBar className="mt-2" percent={p.percent} /></div></div></Panel><div className="grid grid-cols-2 gap-3 sm:grid-cols-4"><StatTile label="Quiz joués" value={player.gamesPlayed} /><StatTile label="Réussite" value={`${successRate}%`} /><StatTile label="Meilleur combo" value={`x${player.bestCombo}`} icon={<Trophy className="size-4" />} accent="warning" /><StatTile label="XP total" value={player.xp} icon={<Zap className="size-4" />} /></div><Panel className="p-5"><SectionTitle title="Mon parcours" /><div className="grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-surface-2 p-4"><p className="text-xs text-muted-foreground">Série</p><p className="mt-1 font-display text-2xl font-black">{player.streakDays} j</p></div><div className="rounded-2xl bg-surface-2 p-4"><p className="text-xs text-muted-foreground">Badges</p><p className="mt-1 font-display text-2xl font-black">{player.badges.length}</p></div><div className="rounded-2xl bg-surface-2 p-4"><p className="text-xs text-muted-foreground">Zones</p><p className="mt-1 font-display text-2xl font-black">{player.zonesCleared.length}/8</p></div></div></Panel></div></AppShell>; }
